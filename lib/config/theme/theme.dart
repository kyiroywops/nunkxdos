import "package:flutter/material.dart";

class AppTheme {
  ThemeData get themeData => ThemeData(
        // esta wea debo ocuparla mejor xd
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
        useMaterial3: true,
      );
       }